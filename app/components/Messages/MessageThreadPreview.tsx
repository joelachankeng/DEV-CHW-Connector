function MessageThreadPreview({
  avatar,
  profileLink,
  name,
  dateTime,
  previewMessage,
  onClick,
  onMarkAsRead,
  onDelete,
  onBlock,
  onReport,
}: {
  avatar: string;
  profileLink: string;
  name: string;
  dateTime: string;
  previewMessage: string;
  onClick: () => void;
  onMarkAsRead: () => void;
  onDelete: () => void;
  onBlock: () => void;
  onReport: () => void;
}) {
  return <></>;
}
